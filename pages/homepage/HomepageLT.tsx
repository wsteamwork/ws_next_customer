import React, { FC, Fragment } from 'react';
import { useTranslation } from 'react-i18next';
interface IProps { };

const HomepageLT: FC<IProps> = (props) => {

  const { t } = useTranslation();

  return (
    <Fragment>

    </Fragment>
  )
}
export default HomepageLT;